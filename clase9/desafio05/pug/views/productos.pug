doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=}, initial-scale=1.0")
        title productos
        link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css")
    body 
        
        h1 productos con pug
        div 
            ul 
                each producto in data
                    li= producto.nombre + " : " +  producto.precio + " - " + producto.img + " - " + producto.id
                   
                            

<div class="mt-3 mr-3 container">
    button  
        a(href="/formulario") formulario
            </div>
                <div class="mt-3 mr-3 container">
                    button  
                        a(href="/") home
                            </div>